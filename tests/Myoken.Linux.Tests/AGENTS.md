# Linux regression scope

Read ../../linux/AGENTS.md before changes. Use isolated temporary profiles and generated fixtures. Never load or overwrite the user's Windows or Linux profile during tests. Run with bash linux/check.sh from the repository root. This console runner covers core/session logic; passing it does not establish GUI or GNOME acceptance.
